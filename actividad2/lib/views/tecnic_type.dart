import 'package:flutter/material.dart';

class TecnicScreen extends StatelessWidget {
  const TecnicScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('OrderScreen'),
      ),
    );
  }
}
